web: node dist/catalogoilmondo/server/main.server.js
```

Esto te mostrará los registros recientes de tu aplicación.

### Verificar la configuración del Procfile

Asegúrate de que tu `Procfile` esté correctamente configurado. Debe estar en la raíz de tu proyecto y contener la línea correcta para iniciar tu aplicación. Por ejemplo:

```markdown
web: node dist/catalogoilmondo/server/main.server.js
```

El error que estás viendo indica que Heroku no puede encontrar el módulo `main.server.js` en la ruta especificada. Esto puede deberse a varios motivos, como una configuración incorrecta del `Procfile` o un problema en el proceso de construcción.

### Solución

1. **Verifica la estructura de tu proyecto**: Asegúrate de que el archivo `main.server.js` exista en la ruta `dist/catalogoilmondo/server/`.

2. **Verificar la estructura del proyecto**: Asegúrate de que el archivo `main.server.js` exista en la ruta `dist/catalogoilmondo/server/`.

3. **Actualizar el `Procfile`**: Asegúrate de que el `Procfile` esté correctamente configurado para iniciar tu aplicación. Aquí hay un ejemplo de cómo debería verse:

```markdown
web: node dist/catalogoilmondo/server/main.server.js
```
